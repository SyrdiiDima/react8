import React from 'react';
import { FormContainer } from 'components/FormContainer/FormContainer';
import { FCont } from 'components/FCont/FCont';

export const ContactsPage = () => {
  return (
    <FCont>
      <FormContainer />
    </FCont>
  );
};
